(ns elephantdb.loader
  (:use [elephantdb util]))


;; use futures

;; returns LP
(defn load-domain-shard [local-shard-path remote-shard-path])

;; returns a map from shard to LP
(defn load-domain [local-domain-root remote-path shards]
  )
