#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py:utf8strings
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import ElephantDBSet
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  bool member(string domain, string setKey, string member)'
  print '  bool members(string domain, string setKey)'
  print '   setDiff(string domain, string keyOne, string keyTwo)'
  print '   setUnion(string domain, string keyOne, string keyTwo)'
  print '   setIntersection(string domain, string keyOne, string keyTwo)'
  print '  i64 size(string domain, string key)'
  print '   multiMember(string domain, string setKey,  setVals)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = ElephantDBSet.Client(protocol)
transport.open()

if cmd == 'member':
  if len(args) != 3:
    print 'member requires 3 args'
    sys.exit(1)
  pp.pprint(client.member(args[0],args[1],args[2],))

elif cmd == 'members':
  if len(args) != 2:
    print 'members requires 2 args'
    sys.exit(1)
  pp.pprint(client.members(args[0],args[1],))

elif cmd == 'setDiff':
  if len(args) != 3:
    print 'setDiff requires 3 args'
    sys.exit(1)
  pp.pprint(client.setDiff(args[0],args[1],args[2],))

elif cmd == 'setUnion':
  if len(args) != 3:
    print 'setUnion requires 3 args'
    sys.exit(1)
  pp.pprint(client.setUnion(args[0],args[1],args[2],))

elif cmd == 'setIntersection':
  if len(args) != 3:
    print 'setIntersection requires 3 args'
    sys.exit(1)
  pp.pprint(client.setIntersection(args[0],args[1],args[2],))

elif cmd == 'size':
  if len(args) != 2:
    print 'size requires 2 args'
    sys.exit(1)
  pp.pprint(client.size(args[0],args[1],))

elif cmd == 'multiMember':
  if len(args) != 3:
    print 'multiMember requires 3 args'
    sys.exit(1)
  pp.pprint(client.multiMember(args[0],args[1],eval(args[2]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
